# Brand DNA — 品牌基因

> ⚠️ **使用前请确认配置：** 下面是默认三色配色，可直接使用。如需换成你自己的品牌色，**改 `tokens.json`**，然后跑 `python scripts/build_tokens.py`，全仓库 HTML 的 `:root` 会自动同步。不要手改任何 HTML 里 `tokens:start` / `tokens:end` 区块之间的内容。头像、气质关键词也请替换成你自己的。

---

## 🎨 IP固定三色（默认配色，可替换为你自己的）

| 色名 | 色值 | 用途 |
|------|------|------|
| 莓果玫红 | `#A63D6F` | 主色调、标题、超链接、重点标记 |
| 藤萝紫 | `#B59AD4` | 强调、装饰、badges、连接线、高亮 |
| 点缀红 | `#E84A5F` | 点缀、高亮下划线、CTA、标签 |

三色比例原则：主色60% · 强调色30% · 点缀色10%（点缀色永远是点缀，不做主色）

> 💡 三色是**视觉身份**，不是可直接当文字色用的调色板。`--accent`（藤萝紫）在暖底上只有 2.40:1，`--pop`（点缀红）3.67:1，都达不到正文标准。每个色都有配套的深调文字版本，见下面的用法矩阵——**写文字时永远查表，不要凭色名猜**。

---

## 📊 完整 Token 用法矩阵

下表由 `tokens.json` 自动生成，是**唯一真值**。任何取色决定都以它为准。

<!-- tokens:matrix:start — generated by scripts/build_tokens.py, do not edit -->

<!-- 由 tokens.json v2.2.0 自动生成，勿手改 -->

| Token | 色值 | 角色 | 对比度 #fefcf6 / #faf6eb | 允许用法 |
|---|---|---|---|---|
| `--brand` | `#A63D6F` | 莓果玫红 · 主色 60% | 5.82 / 5.53 · 正文可用 | 标题、超链接、重点标记、正文强调、按钮底色（配白字） |
| `--brand-deep` | `#88325B` | 莓果深调 · 主色加深 | 7.71 / 7.33 · 正文可用 | hover 态、主色底上的次级层次、小字号强调 |
| `--brand-soft` | `#F4E1EA` | 莓果浅晕 · 浅色块 | 1.22 / 1.16 · 仅装饰/底色 | 高亮底、卡片浅底、tag 浅底（其上文字用 brand / brand-deep / ink） |
| `--accent` | `#B59AD4` | 藤萝紫 · 强调色 30% | 2.40 / 2.28 · 仅装饰/底色 | 装饰、色块、连接线、荷光笔高亮、≥24px 大字、深色面板上的文字 |
| `--accent-ink` | `#885CBA` | 藤萝深调 · 强调色文字安全变体 | 4.77 / 4.54 · 正文可用 | 浅底上的紫色文字、badge/tag 底色（配白字）、图标描边 |
| `--accent-deep` | `#6F46A0` | 藤萝浓调 · 强调色 chip 文字 | 6.69 / 6.36 · 正文可用 | 紫色 wash/soft 底上的小字（accent-ink 在染色底上只有 3.9:1，这个 5.5:1）；也可作填充底配白字 |
| `--accent-soft` | `#EAE2F3` | 藤萝浅晕 · 浅色块 | 1.23 / 1.17 · 仅装饰/底色 | 占位色块、浅底 pill、引用块底（其上文字只用 ink / ink-light / brand-deep） |
| `--pop` | `#E84A5F` | 点缀红 · 点缀色 10% | 3.67 / 3.49 · 仅装饰/底色 | 装饰下划线、角标、≥24px 大字。永远是点缀，不做主色 |
| `--pop-ink` | `#DD1C36` | 点缀深红 · 点缀色文字安全变体 | 4.77 / 4.53 · 正文可用 | 小字号红字、CTA 按钮底色（配白字）、错误/警告文字 |
| `--pop-deep` | `#B00F25` | 点缀浓红 · 点缀色 chip 文字 | 6.96 / 6.61 · 正文可用 | 红色 wash 底上的小字（pop-ink 在染色底上只有 4.2:1，这个 6.1:1）；也可作填充底配白字 |
| `--cream` | `#fefcf6` | 暖底主色 · 背景 | 1.00 / 1.05 · 仅装饰/底色 | 页面主背景；也用作深色面板上的文字色 |
| `--cream-dark` | `#faf6eb` | 深奶底 · 背景 | 1.05 / 1.00 · 仅装饰/底色 | 交替 section 背景、卡片底 |
| `--card-bg` | `#FFFDF8` | 卡片底 · 背景 | 1.01 / 1.06 · 仅装饰/底色 | 卡片/面板底色。不用纯白 #fff |
| `--ink` | `#1A1A2E` | 墨色 · 正文 | 16.63 / 15.80 · 正文可用 | 正文、标题。不用纯黑 #000 |
| `--ink-light` | `#4A4A5A` | 次要墨 · 次要文字 | 8.46 / 8.04 · 正文可用 | 描述文字、副标题 |
| `--ink-faint` | `#6F6F81` | 浅墨 · 辅助文字 | 4.80 / 4.56 · 正文可用 | 注释、caption、页脚（原 #8A8A9A 对比度仅 3.31 已弃用） |
| `--dark-panel` | `#201826` | 梅黑面板 · 深色面板 | 16.78 / 15.94 · 仅装饰/底色 | 全宽深色 section（仅 HTML 全屏页；3:4 卡片场景禁止）。其上文字用 cream / accent |
| `--term-bg` | `#151821` | 终端底 · 具名例外 | 17.28 / 16.42 · 仅装饰/底色 | 仅终端/代码风格组件。冷调暗底在其他场景仍属禁忌 |
| `--term-green` | `#4ADE80` | 终端绿 · 具名例外 | 1.70 / 1.61 · 仅装饰/底色 | 仅终端风格组件的提示符/输出 |
| `--panel-green` | `#2D6A4F` | 功能绿 · 功能色 | 6.23 / 5.92 · 正文可用 | App 场景的特殊功能板块（梦境/自然类），配白字 |
| `--paper` | `#F5F0E8` | 纸感底 · 肌理 | 1.11 / 1.05 · 仅装饰/底色 | 报纸/打字机/机票类组件的纸质底 |
| `--paper-line` | `#D4C9B5` | 纸感线 · 肌理 | 1.60 / 1.52 · 仅装饰/底色 | 虚线撕边、纸质分隔线 |
| `--hairline` | `#E8E4D9` | 细分隔线 · 边框 | 1.24 / 1.18 · 仅装饰/底色 | 1px 分隔线、卡片描边 |

**判定标准**：正文（<24px 常规字重）需 ≥4.5:1，大字（≥24px 或 ≥19px 粗体）需 ≥3.0:1（WCAG 2.1 AA）。

**三条必须记住的替换规则**：

1. 浅底上写紫色字 → 用 `--accent-ink`，不用 `--accent`（`--accent` 在暖底只有 2.40:1）。
2. 深色面板上写紫色字 → 反过来用 `--accent`（在 `--dark-panel` 上 7.00:1），`--accent-ink` 在深底只有 3.51:1。
3. 红色小字或红底白字按钮 → 用 `--pop-ink`（白字 4.89:1），`--pop` 只能做装饰和 ≥24px 大字（白字仅 3.77:1）。

**浅色块上的文字**：`--accent-soft` / `--brand-soft` / `--paper` 之上只用 `--ink`、`--ink-light`、`--brand-deep`；不要用 `--ink-faint` 或 `--accent`。

### 同色系色块（chip / tag / badge）的不透明度上限

色块本身是半透明的，文字真正压在**合成后**的颜色上，不是压在 `#FFFDF8` 上。
下表是每个 wash 在 `--card-bg` 上的合成色，以及配套文字色能否达到 4.5:1。

| 色块 | 当前不透明度 | 合成后底色 | 配套文字 | 对比度 | 判定 |
|---|---|---|---|---|---|
| `--brand-wash` | 0.08 | `#F8EEED` | `--brand-deep` | 6.95 | 通过 |
| `--accent-wash` | 0.25 | `#ECE4EF` | `--accent-deep` | 5.53 | 通过（必须用 `-deep`） |
| `--pop-wash` | 0.12 | `#FCE8E6` | `--pop-deep` | 6.06 | 通过（必须用 `-deep`） |
| `--green-wash` | 0.12 | `#E6EBE4` | `--panel-green` | 5.29 | 通过 |

- `--accent-wash` 上的 `--accent-ink` 只有 3.94:1，**不达标**。`--accent-ink` 想达标，同色系色块的不透明度必须 ≤ 0.08，而当前是 0.25。所以这个色块上的文字一律用 `--accent-deep`（5.53:1）。
- `--pop-wash` 上的 `--pop-ink` 只有 4.15:1，**不达标**。`--pop-ink` 想达标，同色系色块的不透明度必须 ≤ 0.05，而当前是 0.12。所以这个色块上的文字一律用 `--pop-deep`（6.06:1）。

**为什么会有 `-deep` 这一档**：`--accent-ink` / `--pop-ink` 是给**纯暖底**设计的，刚好过线（4.8 左右）。一旦底色被同色系 wash 染过，余量就没了。`--accent-deep` / `--pop-deep` 专门补这个缺口，命名与既有的 `--brand-deep` 一致。

### 代码 / 终端面板：配色禁忌的唯一具名例外

禁忌清单禁止「冷灰蓝调」，但代码高亮是**功能性语义色**，不是装饰。这里显式豁免一套 Tokyo Night 风格暗色语法色，且只允许用在代码/终端面板内部。面板底色固定 `#1A1B26`（代码区）/ `#2D2D3A`（标题栏）。

| Token | 色值 | 在 `#1A1B26` 上 | 在 `#2D2D3A` 上 |
|---|---|---|---|
| `--code-bg` | `#1A1B26` | 面板底色 | 面板底色 |
| `--code-tb` | `#2D2D3A` | 面板底色 | 面板底色 |
| `--code-fg` | `#A9B1D6` | 8.10 | 6.43 |
| `--code-kw` | `#BB9AF7` | 7.39 | 5.86 |
| `--code-fn` | `#7AA2F7` | 6.79 | 5.39 |
| `--code-str` | `#9ECE6A` | 9.35 | 7.42 |
| `--code-num` | `#FF9E64` | 8.40 | 6.67 |
| `--code-cmt` | `#8A94BE` | 5.75 | 4.56 |
| `--code-file` | `#A3A9BD` | 7.30 | 5.79 |

**白底代码面板**（`.code-cl` 这类「极简白底」变体）不能套用上面的暗色语法色——`#7AA2F7` 在白底只有 2.52:1。白底一律用下面这套，全部由既有品牌 token 派生，不引入新色：

| Token | 指向 | 语义 |
|---|---|---|
| `--code-lt-fg` | `var(--ink-light)` · 在 `#FFFDF8` 上 8.54 | 默认文字 |
| `--code-lt-kw` | `var(--accent-ink)` · 在 `#FFFDF8` 上 4.82 | 关键字 |
| `--code-lt-fn` | `var(--brand)` · 在 `#FFFDF8` 上 5.87 | 函数名 / 目录 |
| `--code-lt-str` | `var(--panel-green)` · 在 `#FFFDF8` 上 6.29 | 字符串 |
| `--code-lt-num` | `var(--pop-ink)` · 在 `#FFFDF8` 上 4.81 | 数字 / 常量 |
| `--code-lt-cmt` | `var(--ink-faint)` · 在 `#FFFDF8` 上 4.84 | 注释 |

以上每一项在 `#FFFDF8` 上都 ≥4.5:1。

**逃生舱 `--ds-contrast-ok`**：极少数情况下文字并不压在自身背景上（例如白字压在同级蒙版 `.ov` 上）。此时在该规则里加一条自定义属性说明原因，检查器会跳过它：`--ds-contrast-ok:"白字压在同级 .ov 的 rgba(0,0,0,.85) 蒙版上，非本元素背景"`。写成 CSS 自定义属性而不是注释，因为构建流程会剥掉注释。滥用等于关掉无障碍检查，务必写清理由。

<!-- tokens:matrix:end -->

---

## 👤 头像/IP形象

请将你的头像文件放入 `assets/avatar.jpg`（建议正方形，至少 400×400px）。

如果你有完整IP形象（全身/半身），放入 `assets/character.png`。

### 使用规则
- 需要头像时优先用 `assets/avatar.jpg`
- 是否在页面中使用IP形象由你决定，不强制

---

## 🔤 字体基因

### 核心原则
- **标题用衬线，正文用无衬线** — 混搭产生节奏
- **中英文搭配** — 英文做装饰/标签，中文承载内容
- **字号对比极端** — 大的要很大，小的要真的小

### 推荐字体池

| 场景 | 推荐 | 备注 |
|------|------|------|
| 英文装饰/标题 | `Fraunces` (italic) | 有品质的衬线体 |
| 英文手写/轻松 | `Caveat` | 手绘感、标注、注释 |
| 英文等宽/终端 | `Fira Code` | 技术/终端场景专用 |
| 中文标题 | `Noto Serif SC` (900) | **默认且唯一保证可用的中文衬线标题体** |
| 中文标题（可选升级） | `Huiwen Mincho`（汇文明朝体） | 见下方「关于汇文明朝体」——本仓库不含该字体文件 |
| 中文正文 | `Noto Sans SC` + 系统栈 | 跨平台无衬线 |

### 关于汇文明朝体

汇文明朝体气质更好，但**本仓库不附带该字体文件**（授权原因），且它不在 Google Fonts 上。
因此字体栈里 `'Huiwen Mincho'` 只在**读者本机恰好装了这个字体**时才生效，否则静默回退到 `Noto Serif SC`。

这意味着：

- **不要**假设标题一定是汇文明朝体的字形去做精细的字距/换行微调——大多数人看到的是 `Noto Serif SC`。
- 做视觉验收时以 `Noto Serif SC` 为基准；汇文明朝体当作锦上添花。
- 如果你确实要用，把 ttf 放进 `assets/fonts/`，并在模板里补 `@font-face`（带 `local()` 优先），同时确认你有分发授权。

字体栈以 `tokens.json` 的 `font` 段为准，由构建脚本注入：

```css
--font-serif: 'Huiwen Mincho', 'Noto Serif SC', 'Songti SC', 'STSong', serif;
--font-sans:  'Noto Sans SC', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', system-ui, sans-serif;
--font-display: 'Fraunces', 'Noto Serif SC', serif;
--font-script: 'Caveat', 'Noto Serif SC', cursive;
--font-mono:  'Fira Code', 'SFMono-Regular', Consolas, monospace;
```

**Web 字体只用一个 URL**（`tokens.json` → `font_loading.canonical_url`），不要每个页面各写一串 Google Fonts 参数。

### 字号系统（fluid sizing）

全部走 token，不要写死 rem：

| Token | 值 | 用途 |
|---|---|---|
| `--fs-hero` | `clamp(2.8rem, 7vw, 5.5rem)` | Hero 大标题 |
| `--fs-section-title` | `clamp(1.6rem, 4vw, 2.6rem)` | Section 标题 |
| `--fs-card-title` | `clamp(1.15rem, 2vw, 1.4rem)` | 卡片标题 |
| `--fs-body` | `clamp(0.98rem, 1.6vw, 1rem)` | 正文 |
| `--fs-small` | `clamp(0.78rem, 1.2vw, 0.85rem)` | 辅助文字 |
| `--fs-deco-number` | `clamp(3rem, 8vw, 7rem)` | 大装饰数字（配 `opacity: 0.12~0.2`）|

> ⚠️ **3:4 图文卡片场景例外**：卡片是固定 1080×1440px 导出画布，`vw`/`vh` 会让导出的 PNG 随浏览器窗口变化。卡片场景里用固定 px，不用 `clamp()`。详见下方「响应式通用规则」。

---

## ✨ 气质关键词

设计出来的东西应该让人觉得：

- **可爱但有品质** — 不是幼儿风也不是奢侈风
- **手绘蜡笔感** — 有温度、有人味
- **不像AI** — 这是最高优先级的约束
- **有设计师眼光** — 细节讲究、间距精确、色彩克制
- **温暖但不幼稚** — 有内容有深度
- **个人品牌感** — 一看就知道是"你的"

> 💡 请根据你自己的品牌调性修改上面的关键词。

---

## 🎨 配色扩展原则

当三色不够用时，**先查上面的用法矩阵有没有现成 token**，没有再考虑扩展：

- 背景永远偏暖：`--cream`（主背景）、`--cream-dark`（深奶）、`--card-bg`（卡片底）
- 文字永远非纯黑：用 `--ink`（墨色），次要文字 `--ink-light`，第三级 `--ink-faint`
- 绝不用纯黑 `#000` 或纯白 `#fff`——需要"白"就用 `--card-bg` 或 `--cream`
- 暗色场景底色：`--dark-panel`（梅黑，品牌向）、`--term-bg`（终端底）。**仅适用于 HTML 全屏页面，3:4 卡片场景禁止深色底**
- 终端绿 `--term-green`：仅终端风格场景
- 功能绿 `--panel-green`：需要"成功/正向"语义时用它，<!-- ds-nomigrate:start -->不要临时拿一个 `#28a745` 之类的 Bootstrap 绿（在 `--cream` 上只有 3.05:1，在绿色 chip 底上更低到 2.77:1，当正文色是不合格的）<!-- ds-nomigrate:end -->
- 径向渐变制造层次，不用纯平色

### 深色面板上的取色（容易翻车的地方）

深色面板不是"把浅色方案反过来"那么简单：

- `--brand`（莓果玫红）在 `--dark-panel` 上只有 **2.88:1**，**不能当深底上的文字色**。深底上要玫红感，用 `--brand-soft`（13.77:1）。
- `--accent`（藤萝紫）反而在深底上很好（7.00:1）——和浅底完全相反。
- `--ink` / `--ink-light` 在深底上等于隐形，绝对不要用。

---

## 🚫 通用禁忌清单

| 类型 | 禁止 | 例外 / 边界 |
|------|------|------|
| 配色 | **蓝紫渐变**、cyan、neon、纯黑白、AI常用的**冷灰蓝调大面积铺底**、任何多色渐变背景 | 禁的是**蓝紫渐变**和**冷灰蓝当主色调**，不是禁紫色本身——`--accent` 藤萝紫是品牌三色之一。占位色块用纯色 `--accent-soft` 或 `--accent`；荷光笔高亮 `linear-gradient(transparent 60%, var(--accent) 60%)` 属于硬边高亮，保留。代码/终端面板的语法色是**具名例外**，见用法矩阵里的代码面板表 |
| 深色版面 | 仅3:4图文卡片场景禁止黑色/深色版面 | HTML全屏页面可用深色面板（`--dark-panel` / `--term-bg`）|
| 字体 | Inter/Roboto/Arial/Helvetica Neue/Open Sans/Lato/Montserrat/Poppins 等overused字体、monospace充当"技术感" | 等宽白名单：`Fira Code`/`SFMono-Regular`/`Consolas`/`JetBrains Mono`/`monospace`，仅用于真的在展示代码或终端时 |
| 布局 | 所有section居中、千篇一律卡片网格、cards嵌套cards | — |
| 动效 | bounce/elastic、animate width/height、无限循环动画 | 必须同时提供 `prefers-reduced-motion` 降级（P0）|
| 装饰 | 圆角矩形+阴影千篇一律、渐变文字、AI光效 | **glassmorphism**：作为「通用装饰手法」禁止；固定导航栏的 `backdrop-filter: blur()` 是**具名例外**（组件库 #9），因为它解决的是滚动时文字压在内容上的可读性问题，不是为了好看。除导航栏外不要用 |
| 引用块 | **裸 `border-left` 竖线引用块**（Notion/飞书那种「一条灰竖线 + 缩进」）| 引用块必须从组件库选带完整设计的样式。品牌色条本身不禁止——见下方「品牌色条 vs Notion 竖线」|
| 整体 | 看起来像AI生成的通用模板、generic Landing Page模板感 | — |
| 边框 | 强调色装饰边框细于40px（统一40px，不得更细） | 指**大色块装饰边框**；1px 分隔线用 `--hairline`，引用块色条见下 |
| 排版 | 行间距/字间距必须肉眼检查，不允许出现过松或过紧的异常节奏 | — |
| 图片 | AI生成的stock photo风、过度滤镜、无意义装饰图 | — |
| 默认样式 | HTML默认blockquote、无样式ul/ol列表、默认table——所有组件必须从组件库选用，绝不允许浏览器默认渲染 | 祖先选择器统一接管也算已样式化（如 `.cblock ul { ... }`），不必每个 `<ul>` 都挂 class |

### 品牌色条 vs Notion 竖线（这两条曾经互相矛盾，现在说清楚）

禁忌清单禁的是**"只有一条竖线"**的懒惰引用块；检查清单里的"品牌色条"指的是**有设计的强调条**。区分标准：

| | 禁止 ❌ | 允许 ✅ |
|---|---|---|
| 竖条 | 1~4px 灰色/低饱和竖线 | ≥4px 品牌色（`--brand` / `--accent`）实心条 |
| 其他设计 | 只有 `border-left` + `padding-left`，无背景无字体变化 | 至少再有一项：背景色块、引号装饰、字体切换、错位排版 |
| 观感 | 一眼像 Notion / 飞书 / 语雀 | 一眼能认出是本设计系统 |

检查器（`scripts/lint_design.py` R3）的实现就是这条：只有当选择器名里带 `quote|callout|tip|note|insight` **且**只写了 `border-left` + `padding-left`、没有任何其他视觉设计时才报警。

### 自检问题
做完设计后问自己：
1. 这个页面截图发到社交媒体，会不会被人评论"又是AI做的"？
2. 能不能一眼认出这是你的品牌？
3. 有没有哪个部分让你觉得"见过很多次了"？

---

## 📐 通用间距原则

| Token | 值 |
|---|---|
| `--sp-section` | `clamp(80px, 12vh, 160px)` |
| `--sp-block` | `clamp(40px, 6vw, 100px)` |
| `--sp-card-pad` | `clamp(28px, 3vw, 44px)` |
| `--sp-gap` | `clamp(24px, 3vw, 48px)` |
| `--sp-max-width` | `1300px` |

内容宽度用 `max-width: var(--sp-max-width)` + `margin: 0 auto` 约束。

---

## 📱 响应式通用规则

**HTML 全屏页面（landing / app / tutorial / 组件库）**

- 断点: 900px（两栏→单栏）、600px（字号微缩）
- 移动端是"重新排列"不是"缩小"
- 移动端不隐藏内容——adapt 不 amputate
- 全部走 `clamp()` fluid sizing

**3:4 图文卡片（cards 场景）——完全相反**

卡片是**固定 1080×1440px 的导出画布**，用 `transform: scale()` 预览、`html2canvas` 导出 PNG。

- **禁止** `vw` / `vh`：视口单位会让导出的 PNG 随浏览器窗口尺寸变化，同一份卡片在不同人机器上导出结果不同
- **不要**加 `@media` 断点去"适配手机"——画布尺寸是锁死的
- `clamp()` 也不要用（它内部就是 `vw`）
- 尺寸一律固定 px；比例锁 1080×1440（3:4，容差 ±0.01）

检查器按**场景**分别执行这两套规则，见 `references/checklist.md` 顶部的适用范围表。

**动效降级（所有场景，P0）**

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
  .reveal, [class*="reveal"] { opacity: 1 !important; transform: none !important; }
}
```

最后一条很关键：Scroll Reveal 通常初始 `opacity: 0`，只关掉动画会让内容**永久不可见**。必须显式恢复。

---

## 🔍 细节规范

- **选中文本高亮**: `::selection { background: var(--accent); color: var(--ink); }`（`--ink` 压在 `--accent` 上 6.93:1）
- **链接悬停**: 用强调色底色块或下划线，不用变色
- **键盘焦点**（P0，无障碍）: 必须有可见的 `:focus-visible` 环，不允许 `outline: none` 了事

```css
a:focus-visible, button:focus-visible, [tabindex]:focus-visible {
  outline: 2px solid var(--brand);
  outline-offset: 3px;
  box-shadow: 0 0 0 5px var(--cream), 0 0 0 7px var(--brand-soft);
}
:focus:not(:focus-visible) { outline: none; }
```

---

*This is the foundation. Every scene file builds on top of this.*
